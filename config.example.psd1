@{
    # Optional external model library. If config.psd1 is absent, the default is:
    # <repository root>\models
    ModelsRoot = 'D:\AI\Models'

    # Unsloth Studio listener used by start.ps1.
    Port = 8888
    BindHost = '127.0.0.1'
}
